package main

import (
	"math/rand"
	"time"
)

func main() {
	// Initializing Constants
	const hotelName string = "Gopher Paris Inn"
	const numberOfRooms uint8 = 134
	// Seeding Time
	rand.Seed(time.Now().UnixNano())

}
